{
  "name": "betterbase-examples-e2e",
  "private": true,
  "description": "Lifecycle end-to-end tests for the example apps against the isolated betterbase-e2e stack",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "setup:clients": "bash scripts/setup-clients.sh",    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "check": "tsc -b --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/node": "^24.3.1",
    "typescript": "^5.9.3"
  }
}
